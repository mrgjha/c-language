#include <stdio.h>

/**
 * main - compute the value of z
 *
 * Given two variables
 *
 * Return: 1 if Successful. Otherwise 0.
 */

int main(void)
{
    int x = 3;
    int y = 6;
    int z = x * y == 18;

    printf("The value of z is: %d\n", z);
}
