#include <stdio.h>

/**
 * Description - prints the product of
 * two signed int positive values in long format
 */

int main(void)
{
    signed int x, y;

    x = 255;
    y = 32340;

    printf("%ld\n", x * y);

    return (0);
}
